#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-livereload/bin/grunt-livereload" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-livereload/bin/grunt-livereload" "$@"
  ret=$?
fi
exit $ret
